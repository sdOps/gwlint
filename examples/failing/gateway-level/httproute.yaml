apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: billing-api-route
  namespace: default
spec:
  parentRefs:
    - name: public-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /partner-api
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: billing-api-upstream
          port: 443
