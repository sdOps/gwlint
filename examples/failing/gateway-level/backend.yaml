apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: billing-api-upstream
  namespace: default
spec:
  endpoints:
    - fqdn:
        hostname: billing-api.example.com
        port: 443
