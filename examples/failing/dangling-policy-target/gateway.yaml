apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: dpt-edge-gateway
  namespace: dpt-shop
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      port: 80
      protocol: HTTP
