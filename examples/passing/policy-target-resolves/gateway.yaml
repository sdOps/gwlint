apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: dptok-edge-gateway
  namespace: dptok-shop
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      port: 80
      protocol: HTTP
