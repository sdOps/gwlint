# Attached to "some-other-gateway", not the "checkout-gateway" the policy
# below targets, so the policy's coverage never reaches this route.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: shipping-api-route
  namespace: default
spec:
  parentRefs:
    - name: some-other-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /partner-api
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: shipping-api-upstream
          port: 443
