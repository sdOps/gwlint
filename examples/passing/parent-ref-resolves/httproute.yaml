# The same shape, with the parentRef naming the Gateway that is actually there.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: analytics-route
  namespace: analytics
spec:
  parentRefs:
    - name: analytics-gateway
  rules:
    - backendRefs:
        - name: analytics-api
          port: 8080
