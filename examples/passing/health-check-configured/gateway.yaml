apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: healthcheck-gateway
  namespace: default
spec:
  gatewayClassName: eg
  listeners:
    - name: https
      port: 443
      protocol: HTTPS
